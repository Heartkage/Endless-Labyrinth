using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace gm
{
	public class Player_ItemLists : MonoBehaviour {
        //public bool torch;
        //public bool first_gate_key;
        /*Dictionary<string, bool> itemlists;

        void OnEnable()
        {
            itemlists = new Dictionary<string, bool>();

            //--------All Items in Game--------//
            itemlists.Add(GameManager_GlobalVariables._torch, false);
            itemlists.Add(GameManager_GlobalVariables._firstRoomKey, false);
            itemlists.Add(GameManager_GlobalVariables._secondRoomKey, false);
        }

        public bool AddItem(string name)
        {
            if (itemlists.ContainsKey(name))
            {
                itemlists[name] = true;
                Debug.Log(name + " added to Inventory");
                return true;
            }
            else
            {
                return false;
            }               
        }

        public bool IsItemInInventory(string name)
        {
            return itemlists[name];
        }*/
	}
}

